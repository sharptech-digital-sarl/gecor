# Dépendances de développement / tests
# Installer avec : pip install -r requirements-dev.txt
# (en plus de requirements.txt)

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1                # client HTTP pour TestClient FastAPI

# Lint / format
ruff==0.7.4
black==24.10.0

# Optionnel : couverture
coverage==7.6.4
pytest-cov==6.0.0
